package model;

public enum GameState {

      // Create an enum as we know all possible states before compile time.
      NEW_GAME,
      NEW_ROUND,
      PAUSE,
      CATEGORY_REQUIRED,
      ROUND_COMPLETE,
      GAME_COMPLETE

}
